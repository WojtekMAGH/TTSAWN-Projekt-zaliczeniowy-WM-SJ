<nav class="nav-menu">
    <div class="nav-group">
        <div class="nav-label">MAIN MENU</div>
        <NavLink class="nav-item" href="home" Match="NavLinkMatch.All">
            <i class="fas fa-home"></i>
            <span class="nav-text">Home</span>
        </NavLink>
        <NavLink class="nav-item" href="posts">
            <i class="fas fa-stream"></i>
            <span class="nav-text">Feed</span>
        </NavLink>
        <NavLink class="nav-item" href="create-post">
            <i class="fas fa-pen-to-square"></i>
            <span class="nav-text">Create Post</span>
        </NavLink>
    </div>

    <div class="nav-group">
        <div class="nav-label">ENTERTAINMENT</div>
        <NavLink class="nav-item" href="memory-game">
            <i class="fas fa-gamepad"></i>
            <span class="nav-text">Game</span>
        </NavLink>
    </div>

    <div class="nav-group">
        <div class="nav-label">SUPPORT</div>
        <NavLink class="nav-item" href="contact">
            <i class="fas fa-envelope"></i>
            <span class="nav-text">Contact Us</span>
        </NavLink>
        <NavLink class="nav-item" href="faq">
            <i class="fas fa-circle-question"></i>
            <span class="nav-text">FAQ</span>
        </NavLink>
    </div>
</nav>

<style>
    /* Navigation Menu Container */
    .nav-menu {
        display: flex;
        flex-direction: column;
        padding: 1rem 0.5rem;
        gap: 1.5rem;
        overflow-y: auto;
        height: calc(100vh - 80px); 
    }

    /* Navigation Group */
    .nav-group {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
    }

    /* Navigation Label */
    .nav-label {
        padding: 0.5rem 1rem;
        font-size: 0.75rem;
        font-weight: 700;
        color: #536471;
        letter-spacing: 0.05em;
    }

    /* Navigation Item */
    .nav-item {
        display: flex;
        align-items: center;
        padding: 0.75rem 1.25rem;
        gap: 1.25rem;
        color: #0f1419;
        text-decoration: none;
        transition: all 0.2s ease;
        border-radius: 9999px;
        font-size: 1.1rem;
        font-weight: 500;
    }

        .nav-item i {
            font-size: 1.4rem;
            width: 1.75rem;
            text-align: center;
            color: #536471;
        }

        .nav-item .nav-text {
            font-size: 1.1rem;
        }

        /* Hover State */
        .nav-item:hover {
            background-color: rgba(15, 20, 25, 0.1);
            color: #0f1419;
        }

            .nav-item:hover i {
                color: #1d9bf0;
            }

        /* Active State */
        .nav-item.active {
            font-weight: 700;
            color: #0f1419;
        }

            .nav-item.active i {
                color: #1d9bf0;
            }

    /* Scrollbar Styling */
    .nav-menu::-webkit-scrollbar {
        width: 4px;
    }

    .nav-menu::-webkit-scrollbar-track {
        background: transparent;
    }

    .nav-menu::-webkit-scrollbar-thumb {
        background: #e5e7eb;
        border-radius: 9999px;
    }

    .nav-menu:hover::-webkit-scrollbar-thumb {
        background: #d1d5db;
    }

    /* Collapsed State Styles */
    :global(.collapsed) .nav-label {
        display: none;
    }

    :global(.collapsed) .nav-text {
        display: none;
    }

    :global(.collapsed) .nav-item {
        padding: 0.75rem;
        justify-content: center;
    }

        :global(.collapsed) .nav-item i {
            margin: 0;
            font-size: 1.5rem;
        }
    /* Sidebar */
    .sidebar {
        width: 280px;
        background: #ffffff;
        transition: width 0.3s ease;
        display: flex;
        flex-direction: column;
        z-index: 1000;
        border-right: 1px solid rgb(239, 243, 244);
        padding: 0.5rem;
    }

    .sidebar-header {
        padding: 1rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .toggle-btn {
        width: 34px;
        height: 34px;
        border: none;
        background: transparent;
        border-radius: 9999px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: background-color 0.2s ease;
        color: #536471;
    }

        .toggle-btn:hover {
            background-color: rgba(15, 20, 25, 0.1);
            color: #0f1419;
        }

    .nav-menu {
        padding: 1rem 0.5rem;
        overflow-y: auto;
        height: calc(100vh - 80px);
    }

    /* Navigation Group */
    .nav-group {
        margin-bottom: 1.5rem;
    }

    /* Navigation Label */
    .nav-label {
        padding: 0.75rem 1.5rem;
        font-size: 0.75rem;
        font-weight: 600;
        color: #536471;
        letter-spacing: 0.05em;
    }

    /* Navigation Item */
    .nav-item {
        display: flex;
        align-items: center;
        padding: 0.75rem 1.5rem;
        color: #0f1419;
        text-decoration: none;
        transition: all 0.2s ease;
        border-radius: 9999px;
        font-size: 1.1rem;
        font-weight: 500;
        margin: 0.25rem 0;
    }

        .nav-item i {
            font-size: 1.4rem;
            width: 1.75rem;
            text-align: center;
            color: #536471;
            margin-right: 1.25rem;
        }

        .nav-item .nav-text {
            font-size: 1.1rem;
        }

        /* Hover State */
        .nav-item:hover {
            background-color: rgba(15, 20, 25, 0.1);
            color: #0f1419;
            text-decoration: none;
        }

            .nav-item:hover i {
                color: #1d9bf0;
            }

        /* Active State */
        .nav-item.active {
            font-weight: 700;
            color: #0f1419;
            text-decoration: none;
            background-color: rgba(29, 155, 240, 0.1);
        }

            .nav-item.active i {
                color: #1d9bf0;
            }
</style>

@code {
    private bool collapseNavMenu = true;
    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}